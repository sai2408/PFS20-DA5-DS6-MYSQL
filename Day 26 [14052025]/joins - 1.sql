CREATE DATABASE HELLO25;
USE HELLO25;

CREATE TABLE EMPDETAILS(
	ID INT PRIMARY KEY,
    NAME VARCHAR(20),
    SALARY INT
);
INSERT INTO EMPDETAILS VALUES 
(101,"E1",10000),
(102,"E2",20000),
(103,"E3",30000);
SELECT * FROM EMPDETAILS;

CREATE TABLE MSTATUS(
	ID INT PRIMARY KEY,
    NAME VARCHAR(20),
    STATUSM VARCHAR(10)
);
INSERT INTO MSTATUS VALUES 
(101,"E1","MARRIED"),
(102,"E2","UNMARRIED"),
(104,"E4","MARRIED");
SELECT * FROM MSTATUS;

SELECT EMPDETAILS.ID,MSTATUS.NAME,SALARY,STATUSM
FROM EMPDETAILS
JOIN MSTATUS
ON EMPDETAILS.ID  = MSTATUS.ID;

SELECT EMPDETAILS.ID,EMPDETAILS.NAME,SALARY,STATUSM
FROM EMPDETAILS
LEFT JOIN MSTATUS
ON EMPDETAILS.ID = MSTATUS.ID;

CREATE TABLE EMPLOYEES(
	EMP_ID INT PRIMARY KEY,
    NAME VARCHAR(20) NOT NULL,
    DEPARTMENT_ID INT,
    SALARY INT
);
INSERT INTO employees (emp_id, name, department_id, salary) VALUES
(101, 'Alice', 1, 50000),
(102, 'Bob', 2, 60000),
(103, 'Charlie', 2, 70000),
(104, 'David', 3, 45000),
(105, 'Eve', NULL, 40000),
(106, 'Frank', 4, 55000),
(107, 'Grace', 2, 62000);
SELECT * FROM EMPLOYEES;


CREATE TABLE DEPARTMENTS(
	DEPARTMENT_ID INT PRIMARY KEY,
    DEPARTMENT_NAME VARCHAR(20) NOT NULL
);
INSERT INTO departments (department_id, department_name) VALUES
(1, 'HR'),
(2, 'Engineering'),
(3, 'Marketing'),
(4, 'Finance');
SELECT * FROM DEPARTMENTS;

CREATE TABLE PROJECTS(
	PROJECT_ID INT PRIMARY KEY,
    PROJECT_NAME VARCHAR(20) NOT NULL,
    EMP_ID INT
);
INSERT INTO projects (project_id, project_name, emp_id) VALUES
(201, 'Website Redesign', 102),
(202, 'Mobile App', 103),
(203, 'Ad Campaign', 104),
(204, 'Data Migration', 106),
(205, 'Backend Optimization', 107),
(206, 'Product Launch', NULL); 


SELECT EMP_ID,NAME,DEPARTMENT_NAME
FROM EMPLOYEES
INNER JOIN DEPARTMENTS
ON EMPLOYEES.DEPARTMENT_ID = DEPARTMENTS.DEPARTMENT_ID; 

SELECT EMPLOYEES.EMP_ID,NAME,PROJECT_NAME
FROM EMPLOYEES
INNER JOIN PROJECTS
ON EMPLOYEES.EMP_ID  = PROJECTS.EMP_ID;

SELECT EMP_ID,NAME,DEPARTMENT_NAME
FROM EMPLOYEES
LEFT JOIN DEPARTMENTS
ON EMPLOYEES.DEPARTMENT_ID = DEPARTMENTS.DEPARTMENT_ID; 

SELECT EMPLOYEES.EMP_ID,NAME,PROJECT_NAME
FROM EMPLOYEES
left JOIN PROJECTS
ON EMPLOYEES.EMP_ID  = PROJECTS.EMP_ID;

SELECT DEPARTMENTS.DEPARTMENT_ID,NAME,DEPARTMENT_NAME
FROM DEPARTMENTS
LEFT JOIN EMPLOYEES
ON DEPARTMENTS.DEPARTMENT_ID = EMPLOYEES.DEPARTMENT_ID;